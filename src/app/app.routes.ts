import { Routes } from '@angular/router';
import { ContenidoComponent } from './pages/contenido/contenido.component';

export const routes: Routes = [
    {path: 'pages\contenido', component: ContenidoComponent}
];
